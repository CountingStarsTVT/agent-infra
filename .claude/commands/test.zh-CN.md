---
description: 运行完整测试流程
---

运行项目完整测试流程。

**1. 编译/类型检查**

<!-- TODO: 替换为你的项目编译命令 -->
```bash
# 示例: npx tsc --noEmit / mvn compile / go build ./...
```

**2. 运行所有单元测试**

<!-- TODO: 替换为你的项目测试命令 -->
```bash
# 示例: npm test / mvn test / pytest / go test ./...
```

**3. 输出结果**

报告测试结果摘要。如果测试失败，不要自动修复，等待用户决定。
