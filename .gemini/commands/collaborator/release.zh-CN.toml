description = "执行版本发布流程"
prompt = """
执行版本 {{args}} 的发布流程。

执行步骤:

1. **解析并验证版本号**: 确保 X.Y.Z 格式。

2. **检查工作区状态**: 需要干净的工作目录。

<!-- TODO: 在下方添加你的项目的发布步骤 -->

3. **更新版本引用**:
   ```bash
   # TODO: 替换为你的项目的版本更新命令
   # 示例: 更新 package.json / pom.xml / setup.py / Cargo.toml 版本
   ```

4. **创建发布提交**:
   ```bash
   git add .
   git commit -m "chore: release v{{args}}"
   ```

5. **创建 git 标签**:
   ```bash
   git tag -a v{{args}} -m "Release v{{args}}"
   ```

6. **提供回滚说明** 以防出现问题。

**注意**: 不要自动推送，让用户审查后推送。
"""
