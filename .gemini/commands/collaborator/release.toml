description = "Execute the version release workflow"
prompt = """
Execute version release for {{args}}.

Steps:

1. **Parse and validate version**: Ensure X.Y.Z format.

2. **Check workspace state**: Clean working directory required.

<!-- TODO: Add your project's release steps below -->

3. **Update version references**:
   ```bash
   # TODO: Replace with your project's version update command
   # Example: Update package.json / pom.xml / setup.py / Cargo.toml version
   ```

4. **Create release commit**:
   ```bash
   git add .
   git commit -m "chore: release v{{args}}"
   ```

5. **Create git tag**:
   ```bash
   git tag -a v{{args}} -m "Release v{{args}}"
   ```

6. **Provide rollback instructions** in case of issues.

**Note**: Do NOT push automatically. Let the user review and push.
"""
